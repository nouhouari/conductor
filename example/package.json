{
  "name": "conductor-example",
  "version": "0.1.0",
  "private": true,
  "description": "Example project showing how to use the Conductor test framework",
  "scripts": {
    "test": "cucumber-js",
    "test:web": "cucumber-js --profile web",
    "test:api": "cucumber-js --profile api",
    "test:mobile": "cucumber-js --profile mobile",
    "test:flutter-desktop": "cucumber-js --profile flutterDesktop",
    "test:cross": "cucumber-js --tags @cross-platform",
    "test:dry-run": "cucumber-js --dry-run",
    "fetch:remote": "ts-node --transpile-only ../src/scenarios/cli.ts",
    "test:remote": "npm run fetch:remote && cucumber-js --profile remote",
    "test:remote:dry": "npm run fetch:remote && cucumber-js --profile remote --dry-run",
    "report": "allure generate allure-results --clean -o allure-report",
    "report:open": "allure open allure-report"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.0.0",
    "allure-cucumberjs": "^3.0.0",
    "@nouhouari/conductor-e2e": "file:..",
    "javafx-driver": "^0.2.2",
    "pino": "^9.0.0",
    "pino-pretty": "^11.0.0",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "@cucumber/messages": "^33.0.2",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.4.0"
  }
}
